{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "name": "executionConnector.createPairingCode",
  "description": "Create a one-time pairing code for linking a local execution connector daemon (same as POST /api/v1/execution-connector/pairing-codes). Requires feature.local_execution_connector.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string",
        "description": "User issuing the code (optional, uses session user_id when authenticated)"
      }
    },
    "required": []
  }
}
